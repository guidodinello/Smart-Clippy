{
    "devDependencies": {
        "commander": "^12.1.0",
        "sharp": "^0.33.4",
        "web-ext": "^8.0.0"
    },
    "scripts": {
        "sign": ". ./.env && npx web-ext sign --channel=listed --amo-metadata=metadata.json --api-key=${API_KEY} --api-secret=${API_SECRET}",
        "test": "web-ext run --verbose --devtools",
        "build": "web-ext build --overwrite-dest",
        "lint": "web-ext lint"
    }
}

